import React from 'react';
import PageHeader from '../../components/PageHeader';

import './styles.css'

function SearchList(){
    return(
        <div id="page-search-list" className="container">
            <PageHeader title = "Escolha o melhor petshop para voce"/>
        </div>
    )
}

export default SearchList;